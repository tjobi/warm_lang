# wl README

Language support for `INSERT FINAL LANGUAGE NAME`.

## Features

The extension includes:
- Syntax highlighting for `.wl` files.
- (the rest is still to come :D)

## While under development

To use to extension copy the `warmlang-for-vscode` folder into your 
`{USER-HOME-DIRETORY}/.vscode/extensions/` and reload your vscode window (`CTRL+SHIFT+P` and search for `Reload Window`). 